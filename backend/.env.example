# ⚠️ ATENÇÃO: Este é um arquivo de exemplo!
# Copie para .env e configure com seus valores reais

# Configurações do Banco de Dados
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=coloque_sua_senha_real_aqui
DB_NAME=projetofgt

# Configurações JWT
JWT_SECRET=gere_uma_chave_secreta_forte_aqui
JWT_EXPIRES_IN=24h

# Configurações do Servidor
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:3000

# Configurações do Mercado Pago
MERCADO_PAGO_ACCESS_TOKEN=TEST-your_access_token_here
MERCADO_PAGO_PUBLIC_KEY=TEST-your_public_key_here

# Para produção, use:
# MERCADO_PAGO_ACCESS_TOKEN=APP_USR-your_production_token
# MERCADO_PAGO_PUBLIC_KEY=APP_USR-your_production_public_key

# Configurações de Segurança
BCRYPT_ROUNDS=12
SESSION_SECRET=session_secret_key_aqui

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_LOGIN_MAX=5

# Upload de Arquivos
MAX_FILE_SIZE=10mb
UPLOAD_PATH=./public/uploads

# Configurações de Email (Notificações)
EMAIL_USER=seu_email@gmail.com
EMAIL_PASSWORD=sua_senha_app_gmail
EMAIL_FROM_NAME=Loja de Tênis

# Configurações SMS (Twilio)
TWILIO_SID=seu_twilio_sid
TWILIO_TOKEN=seu_twilio_token
TWILIO_PHONE=+5511999999999

# ⚡ CONFIGURAÇÕES REDIS - SISTEMA DE CACHE
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Para produção Redis, use:
# REDIS_HOST=seu_redis_host
# REDIS_PORT=6379
# REDIS_PASSWORD=sua_senha_redis_forte
# REDIS_DB=0

# Configurações Push (Firebase)
FIREBASE_SERVER_KEY=sua_chave_servidor_firebase

# Configurações de Frete (para futuro)
CORREIOS_USUARIO=seu_usuario_correios
CORREIOS_SENHA=sua_senha_correios

# Log Level
LOG_LEVEL=info

# 📊 CONFIGURAÇÕES DE LOGS AVANÇADOS
LOG_DIR=logs
LOG_MAX_SIZE=20m
LOG_MAX_FILES=14d
LOG_ENABLE_CONSOLE=true
LOG_ENABLE_FILE=true

# 🔧 Para gerar uma chave JWT segura, use:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
